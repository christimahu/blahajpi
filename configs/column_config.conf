# Column configuration for CSV processing
label-column = sentiment_label
text-column = tweet_text

# Add model directory for commands that need it
model-dir = models/demo_model

# Add dataset path for commands that need it
dataset = data/examples/twitter_example.csv
